# Circle-Area-Calculator

A simple Python project that calculates the area of a circle using a user-defined function. It takes the radius as input and uses math.pi to compute the result. This project helps in understanding functions, user input, and mathematical operations.
